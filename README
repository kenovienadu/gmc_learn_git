# Learn Git

This is a dummy project created to practice Git commands. It contains no actual functionality, but it's useful for learning and improving Git workflow.

## Project Purpose

The purpose of this project is to:
- Practice basic Git commands such as `clone`, `add`, `commit`, `push`, `pull`, `branch`, and `merge`.
- Understand Git workflow and collaboration in a version-controlled environment.
- Experiment with different Git features like branches, conflict resolution, and Git history.

## How to Use

1. **Clone the repository:**
   ```bash
   git clone https://github.com/kenovienadu/gmc_learn_git
  ```